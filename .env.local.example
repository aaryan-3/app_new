# Copy this file to .env.local and fill in your Supabase project details
# to enable cloud sync + auth. Without these, the app runs entirely in
# local (this-device-only) mode using the browser's storage — no setup
# required, and nothing about the app breaks.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
